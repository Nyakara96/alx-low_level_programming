#include"main.h"
/**
 * _abs-prints a bsolute value of a number
 * @n:registers an integer
 * Reeturn:1 if above 0,0 if 0 ,and -1 if below 0
 */
int _abs(int);
{
	if (n < 0)
		return (n * -1);
	return (n);
